# README_RUNNING

Живой журнал решений и результатов по проекту.

**English:** [README_RUNNING.md](README_RUNNING.md)

## Правило ведения

- Обновлять после каждой значимой сессии.
- Короткие датированные записи, фокус на решениях.

## 2026-02-25

- Добавлена структура прогресса в `docs/progress/`.
- ОТ построен по остановкам OSM (GTFS недоступен).
- Исправлен сбой запуска из-за отсутствующих координат активностей.
- Исправлено несовпадение расписания в SimWrapper (`.xml` / `.xml.gz`) через свой dashboard provider.
- Настроена политика полос и скоростей сети; пересобраны сеть и ОТ.
- Выполнен расчёт с ОТ и SimWrapper до итерации 10.
- Добавлены маршруты 6, 11, 213, 256 по подсказке пользователя.
- Добавлен `NEW_PC_SETUP.md`, репозиторий перенесён на GitHub с LFS.

## 2026-03-04

- Сверка с MATSim Book и matsim-berlin; чеклист разрывов в `06_gap_checklist_2026-03-04.md`.
- Относительные пути вывода в конфиге `config.xml`; интеграционный тест `RunShamalganIntegrationTest`.
- Выход симуляции унифицирован в папку `output/`.

## 2026-03-06

- Включён локальный DTD по умолчанию (`matsim.preferLocalDtds=true`) — устранён сбой SimWrapper из-за DTD.
- Для стабильной генерации дашбордов использовать `qsim.endTime=30:00:00` (полный день), не `00:30:00`.
- Исключение TripDashboard отменено; причина сбоя — пустой/короткий `output_trips.csv` при коротком endTime.
- Позже (чистка репо): удалены тестовые папки `output-smoke` и `output-smoke-triptest` и все команды, которые в них писали; основной вывод — `output/`.

---

Обновлять одну датированную запись за сессию; формулировки краткие и по делу.
